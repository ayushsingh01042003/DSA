package Heap;

// max heap
public class HeapImpl {
    int[] heapArr;
    int currSize = 0;

    public HeapImpl (int size) {
        this.heapArr = new int[size];
    }

    // add to the end
    // compare and swap with parents to heapify
    public void add (int element) {

    }

    // can only delete root element
    // compare and swap with the greater child node
    public int remove () {

    }

    public int peek () {

    }
}
